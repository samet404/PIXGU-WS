import { runAll } from './utils'
import { admin, base, cr, host, player } from './namespaces'

runAll(
  base,
  host,
  player,
  admin,
  cr,
)

console.log(`------------------------------- \n \n`)
